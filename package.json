{
  "name": "@selectio/root",
  "private": true,
  "engines": {
    "node": "^22.21.0",
    "bun": "^1.3.3"
  },
  "scripts": {
    "build": "turbo run build",
    "clean": "git clean -xdf node_modules",
    "clean:workspaces": "turbo run clean",
    "auth:generate": "bun --filter @selectio/auth generate",
    "db:push": "turbo -F @selectio/db push",
    "db:studio": "turbo -F @selectio/db studio",
    "dev": "turbo watch dev --continue",
    "dev:next": "turbo watch dev -F @selectio/nextjs...",
    "format": "turbo run format --continue -- --cache --cache-location .cache/.prettiercache",
    "format:fix": "turbo run format --continue -- --write --cache --cache-location .cache/.prettiercache",
    "hh:login": "bun --filter @selectio/jobs hh:login",
    "hh:example": "bun --filter @selectio/jobs hh:example",
    "lint": "turbo run lint --continue -- --cache --cache-location .cache/.eslintcache",
    "lint:fix": "turbo run lint --continue -- --fix --cache --cache-location .cache/.eslintcache",
    "lint:ws": "bunx sherif@latest",
    "postinstall": "bun lint:ws",
    "typecheck": "turbo run typecheck",
    "ui-add": "turbo run ui-add"
  },
  "devDependencies": {
    "@selectio/prettier-config": "workspace:*",
    "@turbo/gen": "^2.6.1",
    "dotenv-cli": "^11.0.0",
    "prettier": "^3.6.2",
    "turbo": "^2.6.1",
    "typescript": "^5.9.3"
  },
  "prettier": "@selectio/prettier-config",
  "packageManager": "bun@1.3.3",
  "workspaces": [
    "apps/*",
    "packages/*",
    "tooling/*"
  ]
}
